name: Container CI

on:
  pull_request:
    paths:
      - mavedb_link/**
      - docker/**
      - scripts/**
      - pyproject.toml
      - uv.lock
      - container-release.json
      - vendor/genefoundry/**
      - .github/workflows/container-ci.yml
      - .github/workflows/container-release.yml
      - .github/workflows/data.yml
  push:
    branches:
      - main
    paths:
      - mavedb_link/**
      - docker/**
      - scripts/**
      - pyproject.toml
      - uv.lock
      - container-release.json
      - vendor/genefoundry/**
      - .github/workflows/container-ci.yml
      - .github/workflows/container-release.yml
      - .github/workflows/data.yml

concurrency:
  group: container-ci-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  container-ci:
    uses: berntpopp/genefoundry-router/.github/workflows/_container-ci.yml@6568a0ad7d68925440aba550a2a678282ea5bb6b # genefoundry-router v0.8.7
